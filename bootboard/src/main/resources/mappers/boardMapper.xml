<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gu.mapper.board.BoardMapper">
	<!-- 게시물 목록 -->
	<select id="getBoardList" resultType="com.gu.domain.BoardVO">
		select
			id, title
		from board <!-- ${tableName} -->
	</select>
</mapper>